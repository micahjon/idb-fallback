{
  "name": "idb-fallback",
  "version": "0.1.0",
  "description": "Promise-based IndexedDB wrapper (extends idb-keyval) with memory fallback, versioning, and multiple-tab support.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "idb-keyval": "^3.1.0"
  },
  "devDependencies": {}
}
